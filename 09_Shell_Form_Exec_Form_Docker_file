#-f  give the custome Dockerfile name 
docker build -t slow-app -f Dockerfile.bad .

#give the PID of the countainer
docker top 6674e27822b1

#same give the pid of the container 
#and runing process but somtime ps is not work due to we used low image
docker exec -it 6674e27822b1 ps aux
docker exec -it 6674e27822b1 ps -ef

#this will show the time take to start the container
time docker stop 6674e27822b1

#this means docker start a shell and on that shell start the python so if 
#we send any signal it's taking the time because it's send to the shell firs 
CMD python app.py

# Docker start the python directly and 
#when we send any signal it will send directly on the python
CMD ["python", "app.py"]
   
